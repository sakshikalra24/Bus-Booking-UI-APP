import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#E63946",
    },
    secondary: {
      main: "#1D3557",
    },
    background: {
      default: "#F1FAEE",
    },
  },
});

export default theme;
